Autonomous betting — user installed Grok + plugin, now bet without manual steps.

1. If first time: `wc_arb_onboard`
2. If missing sessions: `wc_arb_login` for stake then cloudbet (user logs in once in browser)
3. If not enabled: `wc_arb_autopilot_enable`
4. Preview: `wc_arb_autopilot(preview=true)`
5. Execute: `wc_arb_autopilot(bankroll=7)`

Or one-shot: `wc_arb_go` — guides or runs when ready.

Never skip login. Never enable without user consent. Report status + screenshots path.