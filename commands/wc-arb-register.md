Show registration links the user still needs. Zero friction.

1. Call MCP `wc_arb_registration_links` (missing_only=true by default).
2. Paste each URL directly in your reply — user should only click and register.
3. Tell user they can paste API keys back in chat for you to save to config.yaml.
4. Env var alternatives: WC_ARB_THE_ODDS_API_KEY, WC_ARB_API_FOOTBALL_KEY, WC_ARB_STAKE_REF.

Do NOT ask user to hunt for registration pages.