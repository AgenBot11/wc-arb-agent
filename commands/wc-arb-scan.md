Scan World Cup arbitrage opportunities now.

1. Call MCP `wc_arb_scan` with bankroll=7 (or user amount), live=true.
2. Output surebets and middles with exact per-platform USDT stakes.
3. If live API missing, tool output includes registration link automatically.
4. Only call `wc_arb_registration_links` if user asks what to register.

Fallback: `python cli.py scan --bankroll 7` from plugin root.